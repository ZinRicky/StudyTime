[project]
name = "studytime-tui"
version = "0.1.0"
description = "Simple TUI program to track and display time spent studying."
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["jsonschema>=4.25.1", "pandas>=2.3.3", "rich>=14.2.0"]
